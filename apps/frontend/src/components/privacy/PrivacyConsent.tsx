import { Link } from 'react-router-dom';
import { ShieldCheck } from 'lucide-react';
import { Button } from '../ui/Button';
import { usePrivacyStore } from '../../store/privacy.store';
export function PrivacyConsent() { const { deviceRiskConsent, setDeviceRiskConsent } = usePrivacyStore(); if (deviceRiskConsent !== null) return null; return <div className="fixed inset-x-4 bottom-4 z-50 mx-auto max-w-2xl rounded-2xl border border-slate-200 bg-white p-5 text-slate-900 shadow-lift"><div className="flex gap-3"><ShieldCheck className="mt-0.5 h-5 w-5 shrink-0 text-accent" /><div><p className="font-extrabold text-primary">Protection contre la fraude</p><p className="mt-1 text-sm leading-5 text-slate-600">Avec votre accord, nous utilisons un identifiant d’appareil pseudonymisé et votre adresse IP lors des mises pour détecter les abus.</p><Link to="/confidentialite" className="mt-2 inline-block text-xs font-bold text-accent hover:underline">Consulter la politique</Link><div className="mt-4 flex gap-2"><Button size="sm" onClick={() => setDeviceRiskConsent(true)}>Accepter</Button><Button size="sm" variant="outline" onClick={() => setDeviceRiskConsent(false)}>Refuser</Button></div></div></div></div>; }
