import { useEffect, useState } from 'react';
import { Bell, Mail, MessageSquare } from 'lucide-react';
import toast from 'react-hot-toast';
import { api } from '../lib/api';
import { Card } from '../components/ui/Card';

export default function CommunicationsPage() { const [items, setItems] = useState<any[]>([]); const [loading, setLoading] = useState(true); useEffect(() => { api.get('/communications/my').then((response: any) => setItems(response.data || [])).catch(() => toast.error('Impossible de charger les communications.')).finally(() => setLoading(false)); }, []); return <div className="mx-auto max-w-5xl px-4 py-10 sm:px-6 lg:px-8"><p className="text-sm font-bold tracking-[.16em] text-accent">ESPACE CLIENT</p><h1 className="mt-2 text-3xl font-extrabold text-primary">Communications</h1><p className="mt-2 text-sm text-slate-600">Historique des alertes dans l’application et des courriels/SMS simulés.</p><div className="mt-8 space-y-3">{loading ? <Card className="p-8 text-center text-slate-600">Chargement…</Card> : items.length ? items.map((item) => <Card key={item.id} className="p-5"><div className="flex items-start gap-3"><Bell className="mt-0.5 h-5 w-5 shrink-0 text-accent" /><div><p className="font-extrabold text-primary">{item.subject}</p><p className="mt-1 text-sm text-slate-700">{item.body}</p><div className="mt-3 flex flex-wrap gap-2 text-xs font-bold"><span className="inline-flex items-center gap-1 rounded-full bg-green-50 px-2.5 py-1 text-green-800"><Bell className="h-3 w-3" />Application envoyée</span><span className="inline-flex items-center gap-1 rounded-full bg-slate-100 px-2.5 py-1 text-slate-700"><Mail className="h-3 w-3" />Courriel simulé</span><span className="inline-flex items-center gap-1 rounded-full bg-slate-100 px-2.5 py-1 text-slate-700"><MessageSquare className="h-3 w-3" />SMS simulé</span></div></div></div></Card>) : <Card className="p-8 text-center text-slate-600">Aucune communication récente.</Card>}</div></div>; }
